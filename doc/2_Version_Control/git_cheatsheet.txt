git commit (saves changes/creates a new environment)
git branch (makes a branch which you can do work on and merge into the main branch)
git checkout (switchs which branch you are working on)
git pull (git fetch + git merge)
git push (pushes changes on your local repository to the remote repository)
git merge (merges the branch you are working on with the branch 
git rebase (creates a HEAD branch under the branch you are rebasing)
git fetch (fetches the branch so that you can use it in the one you are currently working on)
git add
git status
git log
git diff
